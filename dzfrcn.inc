c/    File DZFRCN.INC containing the common blocks for the table
c/    interpolation.
c/    Double Precision version for 32-bit machines

      double precision rec(mq), ainz(mq), tradr(mq), fracz(mq)

      common /zfracs/ rec, ainz, fracz, tradr
